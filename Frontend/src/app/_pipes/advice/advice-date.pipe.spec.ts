import { AdviceDatePipe } from './advice-date.pipe';

describe('AdviceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AdviceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
