import { AdviceEtatPipe } from './advice-etat.pipe';

describe('AdviceEtatPipe', () => {
  it('create an instance', () => {
    const pipe = new AdviceEtatPipe();
    expect(pipe).toBeTruthy();
  });
});
