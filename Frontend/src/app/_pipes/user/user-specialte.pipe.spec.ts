import { UserSpecialtePipe } from './user-specialte.pipe';

describe('UserSpecialtePipe', () => {
  it('create an instance', () => {
    const pipe = new UserSpecialtePipe();
    expect(pipe).toBeTruthy();
  });
});
